# Root reference. Each app has its own env file:
#   backend/.env         (see backend/.env.example)
#   frontend/.env.local  (NEXT_PUBLIC_API_URL=http://localhost:5000)
#
# Local PostgreSQL (docker-compose.yml) defaults:
#   user: rcp_admin  password: rcp_password  db: grancanaria_rcp  port: 5432
